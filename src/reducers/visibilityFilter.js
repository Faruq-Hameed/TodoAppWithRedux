
/**
 * Here the visibility filter reducer takes the state SHOW_ALL and based on the action
 * @param {*} state ; 
 * @param {*} action 
 * @returns  the state based on the action
 */
const visibilityFilter = (state = "SHOW_ALL", action) =>{
    return state
}